# TaskFlow Frontend (React + Tailwind)

## Setup
1. Install dependencies:
```
npm install
```
2. Start dev server:
```
npm run dev
```

By default Vite runs on http://localhost:5173. Make sure backend is running on http://localhost:4000.

